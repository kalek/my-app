import { TabScreen } from '@/components/TabScreen';
import { Text } from 'react-native';

const Insights = () => {
  return (
    <TabScreen>
      <Text>Insights</Text>
    </TabScreen>
  );
};

export default Insights;
